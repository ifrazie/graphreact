## Full-Stack GraphQL and React application
Example social media application

Steps to run the application:
1. npm run client (Starts the React frontend)
2. npm run server (Starts the GraphQL backend)
